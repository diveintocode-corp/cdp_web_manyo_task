name: Manyo self evaluation

on:
  push:
    branches:
      - step1
      - step2
      - step3
      - step4
      - step5
  workflow_dispatch:
    inputs:
      step:
        description: 評価するステップ
        required: true
        type: choice
        options:
          - step1
          - step2
          - step3
          - step4
          - step5
  workflow_call:
    inputs:
      step:
        description: 評価するステップ
        required: true
        type: string
      evaluator_ref:
        description: 固定して使用する評価コードのタグまたはSHA
        required: false
        default: rails-8.1-v1.0.0
        type: string

permissions:
  contents: read

concurrency:
  group: manyo-evaluation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:18.4-alpine
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://postgres@localhost:5432/manyo_test
      MANYO_EVALUATOR_REF: ${{ inputs.evaluator_ref || 'rails-8.1-v1.0.0' }}
      RAILS_ENV: test
      SECRET_KEY_BASE: manyo-evaluation-test

    steps:
      - name: Check out learner application
        uses: actions/checkout@v7

      - name: Set up Ruby and Bundler
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 24.18.0
          cache: yarn

      - name: Install JavaScript dependencies
        run: yarn install --frozen-lockfile

      - name: Run learner tests and requirement evaluation
        run: bin/check "${{ inputs.step || github.ref_name }}"
